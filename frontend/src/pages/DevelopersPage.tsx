import { SdkGenerator } from '../components/SdkGenerator';

/**
 * DevelopersPage Component.
 * Route entry point for the Developer Experience tools.
 * Renders the AI SDK Generator interface.
 */
export const DevelopersPage = () => {
    return <SdkGenerator />;
};
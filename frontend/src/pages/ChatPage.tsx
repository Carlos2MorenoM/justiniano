import { ChatInterface } from '../components/ChatInterface';

/**
 * ChatPage Component.
 * Acts as the route entry point for the main chat functionality.
 * It renders the ChatInterface within the context of the DashboardLayout.
 */
export const ChatPage = () => {
    return <ChatInterface />;
};